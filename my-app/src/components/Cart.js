import React, {useEffect,useState} from 'react';
import axios from 'axios';

const Cart = () => {

  const customerID = 
  
  // Generate a query based on the logged-in state OR prompt if not logged in
  useEffect(()=> {
    axios.get()
  })
  
}

export default Cart;